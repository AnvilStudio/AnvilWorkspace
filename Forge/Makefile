# GNU Make project makefile autogenerated by Premake

ifndef config
  config=debugg
endif

ifndef verbose
  SILENT = @
endif

.PHONY: clean prebuild

SHELLTYPE := posix
ifeq ($(shell echo "test"), "test")
	SHELLTYPE := msdos
endif

# Configurations
# #############################################

ifeq ($(origin CC), default)
  CC = clang
endif
ifeq ($(origin CXX), default)
  CXX = clang++
endif
ifeq ($(origin AR), default)
  AR = ar
endif
RESCOMP = windres
INCLUDES += -I../AnvilEngine/include
FORCE_INCLUDE +=
ALL_CPPFLAGS += $(CPPFLAGS) -MD -MP $(DEFINES) $(INCLUDES)
ALL_RESFLAGS += $(RESFLAGS) $(DEFINES) $(INCLUDES)
LINKCMD = $(CXX) -o "$@" $(OBJECTS) $(RESOURCES) $(ALL_LDFLAGS) $(LIBS)
define PREBUILDCMDS
endef
define PRELINKCMDS
endef

ifeq ($(config),debugg)
TARGETDIR = bin/DebugG-macosx-x86_64/Forge
TARGET = $(TARGETDIR)/Forge
OBJDIR = bin-int/DebugG-macosx-x86_64/Forge
DEFINES += -DDEBUG -DDEBUG_G
ALL_CFLAGS += $(CFLAGS) $(ALL_CPPFLAGS) -m64 -g
ALL_CXXFLAGS += $(CXXFLAGS) $(ALL_CPPFLAGS) -m64 -g -std=c++20
LIBS += ../AnvilEngine/bin/DebugG-macosx-x86_64/AnvilEngine/libAnvilEngine.a ../AnvilEngine/vendor/GLFW/bin/DebugG-macosx-x86_64/GLFW/libGLFW.a -framework Cocoa -framework QuartzCore -framework IOKit -framework Metal -framework CoreFoundation -lvulkan -lshaderc_combined
LDDEPS += ../AnvilEngine/bin/DebugG-macosx-x86_64/AnvilEngine/libAnvilEngine.a ../AnvilEngine/vendor/GLFW/bin/DebugG-macosx-x86_64/GLFW/libGLFW.a
ALL_LDFLAGS += $(LDFLAGS) -L../AnvilEngine/DebugG-macosx-x86_64/AnvilEngine -L../AnvilEngine/vendor/GLFW/DebugG-macosx-x86_64/GLFW -L/usr/local/lib -m64 -Wl,-rpath,@loader_path
define POSTBUILDCMDS
	@echo Running postbuild commands
	install_name_tool -add_rpath "/usr/local/lib" bin/DebugG-macosx-x86_64/Forge/Forge
endef

else ifeq ($(config),debug)
TARGETDIR = bin/Debug-macosx-x86_64/Forge
TARGET = $(TARGETDIR)/Forge
OBJDIR = bin-int/Debug-macosx-x86_64/Forge
DEFINES += -DDEBUG
ALL_CFLAGS += $(CFLAGS) $(ALL_CPPFLAGS) -m64 -g
ALL_CXXFLAGS += $(CXXFLAGS) $(ALL_CPPFLAGS) -m64 -g -std=c++20
LIBS += ../AnvilEngine/bin/Debug-macosx-x86_64/AnvilEngine/libAnvilEngine.a ../AnvilEngine/vendor/GLFW/bin/Debug-macosx-x86_64/GLFW/libGLFW.a -framework Cocoa -framework QuartzCore -framework IOKit -framework Metal -framework CoreFoundation -lvulkan -lshaderc_combined
LDDEPS += ../AnvilEngine/bin/Debug-macosx-x86_64/AnvilEngine/libAnvilEngine.a ../AnvilEngine/vendor/GLFW/bin/Debug-macosx-x86_64/GLFW/libGLFW.a
ALL_LDFLAGS += $(LDFLAGS) -L../AnvilEngine/Debug-macosx-x86_64/AnvilEngine -L../AnvilEngine/vendor/GLFW/Debug-macosx-x86_64/GLFW -L/usr/local/lib -m64 -Wl,-rpath,@loader_path
define POSTBUILDCMDS
	@echo Running postbuild commands
	install_name_tool -add_rpath "/usr/local/lib" bin/Debug-macosx-x86_64/Forge/Forge
endef

else ifeq ($(config),release)
TARGETDIR = bin/Release-macosx-x86_64/Forge
TARGET = $(TARGETDIR)/Forge.app
OBJDIR = bin-int/Release-macosx-x86_64/Forge
DEFINES += -DRELEASE
ALL_CFLAGS += $(CFLAGS) $(ALL_CPPFLAGS) -m64 -O2
ALL_CXXFLAGS += $(CXXFLAGS) $(ALL_CPPFLAGS) -m64 -O2 -std=c++20
LIBS += ../AnvilEngine/bin/Release-macosx-x86_64/AnvilEngine/libAnvilEngine.a ../AnvilEngine/vendor/GLFW/bin/Release-macosx-x86_64/GLFW/libGLFW.a -framework Cocoa -framework QuartzCore -framework IOKit -framework Metal -framework CoreFoundation -lvulkan -lshaderc_combined
LDDEPS += ../AnvilEngine/bin/Release-macosx-x86_64/AnvilEngine/libAnvilEngine.a ../AnvilEngine/vendor/GLFW/bin/Release-macosx-x86_64/GLFW/libGLFW.a
ALL_LDFLAGS += $(LDFLAGS) -L../AnvilEngine/Release-macosx-x86_64/AnvilEngine -L../AnvilEngine/vendor/GLFW/Release-macosx-x86_64/GLFW -L/usr/local/lib -m64 -Wl,-rpath,@loader_path
define POSTBUILDCMDS
	@echo Running postbuild commands
	install_name_tool -add_rpath "/usr/local/lib" bin/Release-macosx-x86_64/Forge/Forge.app
endef

endif

# Per File Configurations
# #############################################


# File sets
# #############################################

GENERATED :=
OBJECTS :=

GENERATED += $(OBJDIR)/Forge.o
OBJECTS += $(OBJDIR)/Forge.o

# Rules
# #############################################

$(TARGET): $(GENERATED) $(OBJECTS) $(LDDEPS) | $(TARGETDIR)
	$(PRELINKCMDS)
	@echo Linking Forge
	$(SILENT) $(LINKCMD)
	$(POSTBUILDCMDS)

$(TARGETDIR):
	@echo Creating $(TARGETDIR)
ifeq (posix,$(SHELLTYPE))
	$(SILENT) mkdir -p $(TARGETDIR)
else
	$(SILENT) mkdir $(subst /,\\,$(TARGETDIR))
endif

$(OBJDIR):
	@echo Creating $(OBJDIR)
ifeq (posix,$(SHELLTYPE))
	$(SILENT) mkdir -p $(OBJDIR)
else
	$(SILENT) mkdir $(subst /,\\,$(OBJDIR))
endif

clean:
	@echo Cleaning Forge
ifeq (posix,$(SHELLTYPE))
	$(SILENT) rm -f  $(TARGET)
	$(SILENT) rm -rf $(GENERATED)
	$(SILENT) rm -rf $(OBJDIR)
else
	$(SILENT) if exist $(subst /,\\,$(TARGET)) del $(subst /,\\,$(TARGET))
	$(SILENT) if exist $(subst /,\\,$(GENERATED)) del /s /q $(subst /,\\,$(GENERATED))
	$(SILENT) if exist $(subst /,\\,$(OBJDIR)) rmdir /s /q $(subst /,\\,$(OBJDIR))
endif

prebuild: | $(OBJDIR)
	$(PREBUILDCMDS)

ifneq (,$(PCH))
$(OBJECTS): $(GCH) | $(PCH_PLACEHOLDER)
$(GCH): $(PCH) | prebuild
	@echo $(notdir $<)
	$(SILENT) $(CXX) -x c++-header $(ALL_CXXFLAGS) -o "$@" -MF "$(@:%.gch=%.d)" -c "$<"
$(PCH_PLACEHOLDER): $(GCH) | $(OBJDIR)
ifeq (posix,$(SHELLTYPE))
	$(SILENT) touch "$@"
else
	$(SILENT) echo $null >> "$@"
endif
else
$(OBJECTS): | prebuild
endif


ifeq ($(config),debugg)
all: $(TARGET)
	@:


else ifeq ($(config),debug)
all: $(TARGET)
	@:


else ifeq ($(config),release)
all: $(TARGET) $(dir $(TARGETDIR))PkgInfo $(dir $(TARGETDIR))Info.plist
	@:

$(dir $(TARGETDIR))PkgInfo:
$(dir $(TARGETDIR))Info.plist:


endif

# File Rules
# #############################################

$(OBJDIR)/Forge.o: Forge.cpp
	@echo "$(notdir $<)"
	$(SILENT) $(CXX) $(ALL_CXXFLAGS) $(FORCE_INCLUDE) -o "$@" -MF "$(@:%.o=%.d)" -c "$<"

-include $(OBJECTS:%.o=%.d)
ifneq (,$(PCH))
  -include $(PCH_PLACEHOLDER).d
endif